// Wikimedia API etiquette asks clients to identify themselves — one
// string for every upstream fetch, so the identity can't drift per file
export const UserAgent = 'landmarks-app/1.0 (https://github.com/eddtb/landmarks; learning project)';
